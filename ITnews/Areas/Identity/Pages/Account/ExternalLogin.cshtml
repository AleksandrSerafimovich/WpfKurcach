@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Register";
}

<h2 localize>Register</h2>
<h4><localize>Свяжите свою учетную запись c</localize> @Model.LoginProvider.</h4>
<hr />

<p class="text-info" localize>
    <localize>Вы успешно прошли аутентификацию с помощью</localize> @Model.LoginProvider.
    <localize>Введите адрес электронной почты для этого сайта ниже и нажмите кнопку «Регистрация», чтобы завершить вход в систему.</localize>
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default" localize>Register</button>
        </form>
    </div>
</div>


